package com.araeosia.Transit;

import org.bukkit.event.Listener;

public class TransitEventListener implements Listener {
	
	Transit plugin;
	
	public TransitEventListener(Transit plugin) {
		this.plugin = plugin;
	}
}
